# 人机协作置信度分层实施方案（可落地版）

本文提供一套可直接执行的人机协作分层方案，目标是实现：

- 高置信度：自动执行
- 中置信度：人工确认
- 低置信度：拒绝并建议人工重试
- 全流程可配置、可审计、可灰度

---

## 1. 适用范围与目标

- **适用对象**：工具调用（含内置工具、MCP 工具、可执行命令类工具）
- **核心目标**：
  - 降低高风险误执行
  - 维持可接受的自动化率
  - 将不确定场景稳定路由到人工确认
- **设计原则**：
  - 默认保守（宁可确认，不盲目自动）
  - 策略可解释（每次决策可追溯）
  - 阈值可运营（按环境/工具/租户可调）

---

## 2. 决策分层模型

### 2.1 三档决策定义

- `AUTO_EXECUTE`：自动执行
- `HUMAN_CONFIRM`：进入人工确认
- `REJECT_RETRY`：拒绝执行并提示重试

### 2.2 评分输入（建议最小集）

- `model_confidence`：模型对当前动作判断置信度（0~1）
- `risk_score`：动作风险评分（0~1，越高越危险）
- `context_quality`：上下文完整性评分（0~1）
- `policy_match_score`：策略匹配可靠度（0~1）

### 2.3 汇总评分（建议）

```text
final_confidence =
  w_model * model_confidence
  + w_inverse_risk * (1 - risk_score)
  + w_context * context_quality
  + w_policy * policy_match_score
```

说明：
- 先用线性模型，便于调参与解释。
- 后续可逐步升级为学习型模型，但输出仍映射到同一三档策略。

---

## 3. 实施清单（可直接排期）

## 阶段 A：策略准备（1-2 天）

- 明确高风险工具清单（如 `bash`、`write_file`、`mcp:*:delete*`）。
- 明确强制拒绝模式（如 `rm -rf`、`drop table`、`truncate`）。
- 定义分环境阈值（`dev/staging/prod`）。
- 定义例外规则（按工具组/租户/部门）。

验收标准：
- 风险工具与危险模式名单评审通过。
- 阈值初版在文档中冻结（具版本号）。

## 阶段 B：观测优先（Shadow 模式，3-7 天）

- 上线打分逻辑，但不拦截执行（仅记录建议决策）。
- 全量记录审计日志（含输入、子分值、最终建议档位）。
- 复盘误判样本，调整权重与阈值。

验收标准：
- 日志覆盖率 >= 99%。
- 关键字段无空值（阈值快照、决策理由、评分拆分）。

## 阶段 C：小流量生效（灰度 Enforce，3-7 天）

- 对 10%-30% 流量开启三档执行。
- 中置信触发人工确认，低置信拒绝并提示补充信息。
- 建立回滚开关（全局降级到 `HUMAN_CONFIRM` 或 `SHADOW`）。

验收标准：
- 高风险工具误自动执行率接近 0。
- 人工确认链路稳定（超时、取消、重试可观测）。

## 阶段 D：全量与运营（持续）

- 全量启用后按周复盘：
  - 人工推翻率（系统建议 vs 人工结果）
  - 低置信拒绝后二次成功率
  - 工具组自动化率
- 按工具组逐步精调阈值。
- 形成阈值版本管理机制（可回滚）。

验收标准：
- 指标稳定，误判趋势下降。
- 配置变更有记录、可审计、可追溯。

---

## 4. 参数默认值表（建议起步值）

> 说明：以下为默认建议，需按业务风险接受度微调。

| 参数 | 默认值 | 取值范围 | 说明 |
|---|---:|---|---|
| `enabled` | `true` | `true/false` | 总开关 |
| `mode` | `shadow` | `shadow/enforce` | 运行模式 |
| `auto_execute_min` | `0.80` | `0~1` | 高置信度下限 |
| `human_confirm_min` | `0.50` | `0~1` | 中置信度下限；低于即拒绝 |
| `w_model` | `0.35` | `0~1` | 模型置信度权重 |
| `w_inverse_risk` | `0.35` | `0~1` | 风险反向权重 |
| `w_context` | `0.20` | `0~1` | 上下文完整性权重 |
| `w_policy` | `0.10` | `0~1` | 策略匹配权重 |
| `sample_rate` | `0.20` | `0~1` | 灰度抽样比例 |
| `on_evaluator_error` | `human_confirm` | `human_confirm/reject_retry` | 评估器异常回退动作 |
| `confirm_timeout_sec` | `120` | 正整数 | 人工确认超时 |
| `reject_message_template` | 见下 | 字符串 | 低置信拒绝文案模板 |

约束建议：
- 权重和应为 `1.0`（容差 `±0.001`）。
- 阈值应满足：`auto_execute_min > human_confirm_min`。

### 4.1 高风险工具组推荐阈值

| 工具组 | `auto_execute_min` | `human_confirm_min` | 备注 |
|---|---:|---:|---|
| 普通只读工具 | 0.80 | 0.50 | 如 `read_file`、`web_search` |
| 写操作工具 | 0.90 | 0.70 | 如 `write_file`、`str_replace` |
| 命令执行工具 | 0.95 | 0.80 | 如 `bash` |
| 破坏性 MCP 操作 | 1.00 | 0.95 | 未达阈值默认拒绝 |

### 4.2 建议配置片段（文档示例）

```yaml
human_collab:
  enabled: true
  mode: shadow
  version: v1

  thresholds:
    auto_execute_min: 0.80
    human_confirm_min: 0.50

  weights:
    w_model: 0.35
    w_inverse_risk: 0.35
    w_context: 0.20
    w_policy: 0.10

  risk_overrides:
    force_human_confirm_tools:
      - bash
      - write_file
      - "mcp:*:delete*"
    force_reject_patterns:
      - "rm -rf"
      - "drop table"
      - "truncate "

  runtime:
    sample_rate: 0.20
    confirm_timeout_sec: 120
    on_evaluator_error: human_confirm
```

---

## 5. 决策与执行流程（标准化）

1. 计算子分值与 `final_confidence`。
2. 先执行强规则覆盖：
   - 命中 `force_reject_patterns` -> `REJECT_RETRY`
   - 命中 `force_human_confirm_tools` -> `HUMAN_CONFIRM`
3. 否则按阈值路由：
   - `final_confidence >= auto_execute_min` -> `AUTO_EXECUTE`
   - `human_confirm_min <= final_confidence < auto_execute_min` -> `HUMAN_CONFIRM`
   - `< human_confirm_min` -> `REJECT_RETRY`
4. 写入审计日志（包含阈值快照、规则命中、决策理由）。

---

## 6. 人工确认与拒绝文案建议

### 6.1 人工确认提示模板

```text
该操作存在中等不确定性，建议人工确认后执行。
- 工具: {tool_name}
- 关键参数: {key_args}
- 评分: {final_confidence}
- 风险点: {risk_reasons}
请确认是否继续。
```

### 6.2 低置信拒绝提示模板

```text
当前请求置信度较低，系统已拒绝自动执行。
建议你补充以下信息后重试：
1) 目标范围（文件/表/环境）
2) 预期结果与边界条件
3) 可接受风险等级
```

---

## 7. 审计字段 DDL 草案（PostgreSQL）

> 用于记录每次分层决策与最终执行结果，支持后续复盘和调参。

```sql
create table if not exists human_collab_decision_logs (
  id                         bigserial primary key,
  request_id                 text not null,
  thread_id                  text,
  user_id                    text,
  tenant_id                  text,
  agent_id                   text,

  tool_name                  text not null,
  tool_input_hash            text,              -- 参数摘要（建议 hash，避免敏感信息直存）
  action                     text,              -- read/write/execute/...

  decision                   text not null check (decision in ('AUTO_EXECUTE', 'HUMAN_CONFIRM', 'REJECT_RETRY')),
  final_confidence           numeric(5,4) not null check (final_confidence >= 0 and final_confidence <= 1),

  model_confidence           numeric(5,4) check (model_confidence >= 0 and model_confidence <= 1),
  risk_score                 numeric(5,4) check (risk_score >= 0 and risk_score <= 1),
  context_quality            numeric(5,4) check (context_quality >= 0 and context_quality <= 1),
  policy_match_score         numeric(5,4) check (policy_match_score >= 0 and policy_match_score <= 1),

  auto_execute_min           numeric(5,4) not null check (auto_execute_min >= 0 and auto_execute_min <= 1),
  human_confirm_min          numeric(5,4) not null check (human_confirm_min >= 0 and human_confirm_min <= 1),
  threshold_version          text not null,     -- 例如 v1 / v1.2
  mode                       text not null check (mode in ('SHADOW', 'ENFORCE')),

  forced_by_rule             boolean not null default false,
  matched_rules              jsonb not null default '[]'::jsonb,  -- 命中的强规则列表
  reasons                    jsonb not null default '[]'::jsonb,  -- 可解释原因列表

  human_confirmation_status  text check (human_confirmation_status in ('PENDING', 'APPROVED', 'REJECTED', 'TIMEOUT', 'SKIPPED')),
  human_confirmation_by      text,
  human_confirmation_at      timestamptz,

  execution_status           text check (execution_status in ('NOT_EXECUTED', 'SUCCESS', 'FAILED')),
  execution_error_code       text,
  execution_error_message    text,

  latency_ms                 integer,           -- 从评估到决策/执行结束的耗时
  created_at                 timestamptz not null default now()
);
```

### 7.1 索引建议

```sql
create index if not exists idx_hcdl_created_at
  on human_collab_decision_logs(created_at desc);

create index if not exists idx_hcdl_request_id
  on human_collab_decision_logs(request_id);

create index if not exists idx_hcdl_user_time
  on human_collab_decision_logs(user_id, created_at desc);

create index if not exists idx_hcdl_tool_decision_time
  on human_collab_decision_logs(tool_name, decision, created_at desc);

create index if not exists idx_hcdl_mode_version
  on human_collab_decision_logs(mode, threshold_version, created_at desc);
```

### 7.2 可选分区建议

- 日志量大时建议按月分区：`created_at`。
- 保留策略：在线保留 90~180 天，历史归档至对象存储/数仓。

---

## 8. 运营看板指标（上线后必看）

- 自动执行率（按工具组）
- 人工确认率与确认耗时（P50/P95）
- 低置信拒绝率
- 人工推翻率（系统建议与人工决策不一致）
- 高风险工具误自动执行率（核心安全指标）
- 低置信拒绝后二次成功率（提示是否有效）

---

## 9. 上线与回滚策略

### 9.1 上线策略

- 第 1 周：`SHADOW` 全量观测
- 第 2 周：`ENFORCE` 10%-30% 灰度
- 第 3 周：扩至 50%-100%，按指标逐步放量

### 9.2 回滚策略

任一条件触发可回滚到 `SHADOW` 或强制 `HUMAN_CONFIRM`：

- 高风险误自动执行率超阈值
- 人工确认链路异常（确认不可用/超时激增）
- 拒绝率异常飙升影响核心业务

---

## 10. 变更治理建议

- 阈值调整必须带版本号（`threshold_version`）。
- 每次调整记录：
  - 调整原因
  - 预期影响指标
  - 回滚条件
- 评估器异常默认走保守路径：`HUMAN_CONFIRM`（必要时 `REJECT_RETRY`）。

---

## 11. 一句话执行摘要

先以 **Shadow 观测** 校准阈值，再灰度开启“高自动、中确认、低拒绝”，用统一审计表闭环评估并持续调参，实现安全与效率的平衡。

