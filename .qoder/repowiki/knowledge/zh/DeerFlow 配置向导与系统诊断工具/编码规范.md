- 所有 Python 脚本均使用 `#!/usr/bin/env python3` Shebang 并遵循 `from __future__ import annotations` 的现代类型注解规范。
- UI 交互组件统一采用 ANSI 转义码实现彩色输出，并提供 `_supports_color()` 检测以确保在非 TTY 环境下的兼容性。
- 配置写入逻辑优先读取 `config.example.yaml` 获取最新 `config_version`，确保生成的配置文件与项目当前版本保持同步。