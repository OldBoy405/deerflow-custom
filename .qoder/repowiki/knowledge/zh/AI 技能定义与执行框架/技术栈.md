- **多语言运行时**：混合使用 Node.js (>=18.0.0) 处理前端可视化任务（如 `generate.js`），Python 3 处理数据分析与文件操作（如 `download_images.py`）。
- **可视化引擎**：集成 AntV GPT-Vis API (`antv-studio.alipay.com`) 进行专业图表渲染。
- **MCP 协议支持**：部分自定义技能（如 `label-verification`）支持 Model Context Protocol，实现与 MES/QMS 系统的快速路径对接。