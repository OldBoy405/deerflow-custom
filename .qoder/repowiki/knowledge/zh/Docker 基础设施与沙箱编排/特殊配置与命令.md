- **开发环境启动**：使用 `docker-compose -f docker-compose-dev.yaml up --build`，支持源码热重载。
- **沙箱模式配置**：需挂载宿主机的 `~/.kube/config` 并设置 `K8S_API_SERVER` 以允许容器访问宿主机 K8s API。
- **依赖自修复**：`dev-entrypoint.sh` 脚本在 Gateway 启动时自动执行 `uv sync --all-packages`，并在失败时重建 `.venv`。