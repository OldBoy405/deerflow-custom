- 所有服务间通信均通过 Docker 内部网络名称（如 `gateway:8001`）进行解析，避免硬编码 IP。
- Nginx 配置中针对 `/api/langgraph/` 等特定路径进行 URL 重写，以兼容 LangGraph 接口规范。
- Provisioner 采用“先查后建”的幂等设计，在创建沙箱前检查现有 Service 是否存在以避免资源冲突。
- 环境变量命名遵循 `DEER_FLOW_` 前缀规范，并通过 `.env` 文件统一管理跨服务的配置参数。