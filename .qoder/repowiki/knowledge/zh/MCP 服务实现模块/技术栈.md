- 视觉模型集成：通过 `urllib` 直接调用兼容 OpenAI 格式的 Vision API（如 Qwen-VL）进行图像比对。
- 向量检索：使用 `pgvector` 扩展的 PostgreSQL 数据库，配合 `psycopg` 驱动执行余弦相似度搜索。
- 数据库访问：使用 `SQLAlchemy` 核心模式（Core）配合 `pyodbc` 驱动连接 SQL Server，确保只读安全。