- **环境初始化**：使用 `uv sync` 安装依赖，支持 `postgres` 和 `discord` 可选依赖组。
- **测试门禁**：集成 `blockbuster` 进行阻塞 I/O 检测，通过 `pytest -m no_auto_user` 等标记控制测试上下文。
- **启动命令**：通过 `uvicorn app.gateway.app:app` 启动网关，支持 `config.yaml` 热重载配置。