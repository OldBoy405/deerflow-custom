- 中间件链采用声明式特征驱动组装，通过 `@Next`/`@Prev` 锚点实现自定义中间件的精确插入。
- 持久化组件（Checkpointer/Store）采用单例模式与上下文管理器双轨设计，兼顾长驻服务与一次性脚本的资源管理。
- 智能体状态更新遵循不可变原则，中间件通过返回字典片段由 LangGraph Reducer 合并状态，避免直接修改 State 对象。
- 用户身份通过 `ContextVar` 在线程间传递，并在异步任务启动前显式捕获，确保后台任务中的权限隔离一致性。